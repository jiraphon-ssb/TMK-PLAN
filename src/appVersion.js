/* ============================================================
   appVersion.js — เวอร์ชันแอป (สตริงสั้น ๆ ตัวเดียว)
   ============================================================
   ทำไมต้องแยกจาก changelog.js:
     changelog.js มีข้อความ What's New ทุกเวอร์ชันย้อนหลัง = ~216 KB
     และ App.jsx import WhatsNew.jsx แบบ static → ทั้งก้อนตกอยู่ใน entry chunk
     = ทุกคนต้องโหลดข้อความที่ยังไม่ได้เปิดอ่าน ก่อนเห็นหน้าจอแรก (≈ 1/3 ของไฟล์แรก)
     สิ่งที่ต้องรู้ตั้งแต่ต้นมีแค่ "เวอร์ชันปัจจุบัน" (ไว้เทียบจุดแดง "ยังไม่อ่าน")
   ⚠️ ต้องตรงกับ CHANGELOG[0].ver เสมอ — มีเทสบังคับที่ lib/__tests__/appVersion.test.js
   ============================================================ */
export const APP_VERSION = '3.34.0';
